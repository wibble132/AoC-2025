# Rust!

Rust is cool, much more so than [C++](../cpp).

To run this, you need Rust, google Rustup and install it.

- Run with `cargo run` or a specific day with `cargo run -- N`
- Test with `cargo test`
- Benchmark with `cargo bench`
